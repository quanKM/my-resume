import React, { FC } from 'react'

type Props = {
  description?: string
  meta?: any
  title: string
}

const SEO: FC<Props> = ({ description, meta, title }) => {
  return <div></div>
}

export default SEO
